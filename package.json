{
  "name": "kaizen-agent",
  "version": "0.1.1",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "author": "Enrique Rocha (@soyEnriqueRocha)",
  "contributors": [
    "@tododeia"
  ],
  "homepage": "https://github.com/soyEnriqueRocha",
  "license": "MIT",
  "main": "dist/entry.js",
  "bin": {
    "kaizen": "./kaizen.mjs"
  },
  "scripts": {
    "build": "pnpm run build:cli && pnpm run ui:build",
    "build:cli": "tsc -p tsconfig.json",
    "ui:build": "pnpm --dir ui build",
    "ui:dev": "pnpm --dir ui dev",
    "dev": "node --import tsx ./src/entry.ts",
    "start": "pnpm run build && node ./kaizen.mjs"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "commander": "^14.0.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
